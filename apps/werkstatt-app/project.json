{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/werkstatt-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/werkstatt-app",
        "outputPath": "dist/apps/werkstatt-app"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/werkstatt-app"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "werkstatt-app:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "werkstatt-app:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "werkstatt-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "werkstatt-app:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/werkstatt-app"],
      "options": {
        "jestConfig": "apps/werkstatt-app/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/werkstatt-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
